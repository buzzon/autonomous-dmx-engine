{
  "name": "dmx-engine-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:legacy": "esbuild main.ts --bundle --outfile=main.js --format=esm"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@zhobo63/imgui-ts": "^0.1.52",
    "socket.io-client": "^4.8.3"
  }
}